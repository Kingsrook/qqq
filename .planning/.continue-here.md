---
context: research
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-04-22T15:05:30.059Z
workflow_in_progress: /gsd-new-project (paused at Step 9 wrap-up)
---

# Resume QQQ 4.0 GSD initialization

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Skill auto-invocation on general-purpose agents | First BOM-audit spawn (agent `a33c54037a25a945b`) returned asking for `~/.claude/projects/` transcript-scanning permissions — the `fewer-permission-prompts` skill auto-fired instead of executing the task. | advisory | For finite file-reading work, prefer inline tool calls over general-purpose agent spawns. If spawning, include explicit "do only this task, do not invoke any skills" guidance. |
| Meridian-family subagent without explicit model | First research spawn with `subagent_type=researcher` failed: `gpt-5.4-mini` not available. Meridian agents default to a different model family not available here. | advisory | Use `subagent_type=general-purpose` + `model=sonnet` for external web research. Avoid meridian-family agents (researcher, brainstormer, etc.) in non-meridian projects. |

<current_state>
Mid-`/gsd-new-project` workflow. Steps 1–8 complete and committed. Step 9 (wrap-up — CLAUDE.md generation + "Next Up" presentation) paused mid-execution because the user surfaced a broader strategic question: ~40 sibling repos (QBits, frontends, starters, qctl, marketing, docs) exist outside this mono, and the versioning/coordination story is painful. Two parallel inquiries were launched to inform a scope decision: (1) **BOM audit** — ✅ complete at `.planning/research/bom-audit.md`, uncommitted; (2) **Multi-repo patterns research** — ❌ blocked on denied `WebSearch` + `WebFetch` permissions.

The scope decision deferred by the user: "should downstream sibling Java repos (QBits, starters, qctl) be updated to run on 4.0 as part of this milestone, or ship qqq only and let downstreams migrate post-release using the migration guide?"
</current_state>

<completed_work>

Committed this session:
- `068779caf` — `docs: initialize project` — `.planning/PROJECT.md` (14 inferred Validated reqs, 13 Active for milestone 1, reorg queued for milestone 2)
- `76f486b09` — `chore: add project config` — `.planning/config.json` (yolo / standard / parallel / balanced / research+plan-check+verifier all on / commit_docs=true)
- `a6ec988c7` — `docs: define v1 requirements` — `.planning/REQUIREMENTS.md` (13 v1 reqs: STAB-01..04, BREAK-01..04, REL-01..02, DOC-01..02, SAMP-01)
- `38c7caf9c` — `docs: create roadmap (8 phases)` — `.planning/ROADMAP.md` + `.planning/STATE.md` + traceability in REQUIREMENTS.md

Written but uncommitted:
- `.planning/research/bom-audit.md` — facts-only audit of `qqq-bom` coverage gaps and ecosystem version drift

Done (decisions / gates):
- Skipped project-level 4-agent research fan-out (`.planning/codebase/*` already covers the domain)
- Skipped sub-repo detection (no nested `.git` in this repo's direct children)
- Deep questioning complete (see decisions_made in `HANDOFF.json`)
- `~/.gsd/defaults.json` only had `resolve_model_ids` — went through fresh config

</completed_work>

<remaining_work>

1. **Unblock multi-repo patterns research.** Check whether user has granted `WebSearch` + `WebFetch` permissions. If yes, re-spawn the research agent. If no, prompt user to grant them.
2. **Summarize patterns research + resume scope discussion.** Once research lands, combine it with the BOM audit and re-ask the deferred question: downstream sibling repos in 4.0 scope or not? (and its sub-question: update PROJECT.md Context now or at `/gsd-new-milestone`?)
3. **Potentially revise REQUIREMENTS.md + ROADMAP.md** if the scope decision brings downstream repos into milestone 1. May add a phase (Downstream Consumer Sweep) or augment STAB-04's partner validation to include named sibling repos.
4. **Commit `.planning/research/bom-audit.md`.** Hasn't been committed yet; group it with the patterns report when that lands.
5. **Generate project-level `CLAUDE.md`.** `gsd-sdk query generate-claude-md --output CLAUDE.md` does NOT write a file — only returns JSON sections. Workaround: write `CLAUDE.md` manually assembling from `$HOME/.claude/get-shit-done/templates/claude-md.md` (7 marker-bounded sections) + `.planning/codebase/*.md` + `.planning/PROJECT.md`.
6. **Final commit** — group `CLAUDE.md` + any scope-decision-driven revisions in one commit per the Step 8 workflow: `gsd-sdk query commit "docs: create roadmap (8 phases)" ...` already ran once but didn't include `CLAUDE.md` (file wasn't written). A follow-up commit is needed.
7. **Present the Step 9 "Next Up" block** for Phase 1 (`UI hint: no` for all phases per Phase 1 detail, so use the non-UI version of the Next Up template).

</remaining_work>

<decisions_made>

See `HANDOFF.json` → `decisions_made` for full list. Headline set:

- Target version **4.0.0** (not 0.42.0) — version-number correction + semver-contract inflection
- Fold breaking renames (backend.javalin → middleware.javalin, frontend.picocli → middleware.picocli) + full `@Deprecated` sweep + targeted API-shape cleanups **into 4.0** rather than a future major
- Include `qqq-sample-project` update on current qfmd in 4.0 scope (overrode an earlier "external/partner apps only" answer)
- Hold JaCoCo baseline — no regression, no raise
- Reorg (QBit + UI mono-repo restructure) **deferred to milestone 2** via `/gsd-new-milestone`
- Config: YOLO / standard granularity / parallel / balanced models / research + plan-check + verifier agents all on

</decisions_made>

<blockers>

| Blocker | Type | Workaround |
|---|---|---|
| `WebSearch` + `WebFetch` permissions denied | human_action | Grant via `/permissions` or settings.json; then "retry research" |
| `gsd-sdk query generate-claude-md --output <path>` doesn't write a file (only returns JSON sections) | technical | Write `CLAUDE.md` manually from template + codebase intel files |
| Downstream-sibling-scope decision for milestone 1 pending the patterns research | blocked_on_research | Work from BOM audit alone if patterns research stays blocked |

</blockers>

## Required Reading (in order)

1. `.planning/PROJECT.md` — 4.0 milestone scope, Core Value, Validated/Active/Out-of-Scope, Constraints, Key Decisions
2. `.planning/REQUIREMENTS.md` — 13 v1 requirements with REQ-IDs, traceability table
3. `.planning/ROADMAP.md` — 8-phase structure with dependencies for parallel execution
4. `.planning/STATE.md` — project-memory pointer
5. `.planning/research/bom-audit.md` — ecosystem version drift facts (informs scope decision)
6. `.planning/HANDOFF.json` — machine-readable state; full decision history
7. `$HOME/.claude/get-shit-done/workflows/new-project.md` — workflow being resumed; Step 9 is where we paused
8. `$HOME/.claude/get-shit-done/templates/claude-md.md` — template for the manually-generated CLAUDE.md

## Infrastructure State

- **Git:** develop branch, clean working tree except `.planning/research/` is untracked (contains the uncommitted bom-audit.md)
- **Research agent `acaf13450bac145ee`:** completed with blocker — no file written. Safe to abandon / re-spawn.
- **Research agent `abce6914859b91be7`:** first attempt with subagent_type=researcher — failed with model-resolution error. Abandoned.
- **BOM-audit agent `a33c54037a25a945b`:** completed with skill-routing confusion; no file written. Audit was done inline instead. Safe to abandon.
- **No servers running**, no background tasks, no watchers

<context>

User originally framed the work as two staged tracks: (1) stabilize & publish a new version, (2) later reorganize QBits + external UIs into a better mono-repo structure. In `/gsd-new-project`, we scoped milestone 1 tightly around (1), planning to open (2) later via `/gsd-new-milestone`. During Step 9 wrap-up, the user raised a key clarification: the ecosystem ISN'T this one repo — it's ~40 sibling repos at `/Users/james.maes/Git.Local/qrun-io/` (20+ QBits, 3 frontends, 2 starters, qctl, CircleCI orb, IntelliJ plugins, marketing, docs).

That reframes both (a) what "shipping 4.0 stably" means (does it include downstream fixes?) and (b) how big milestone 2's reorg really is (consolidation question spans 40+ repos across 4 groupIds and 2 languages).

I opened two concurrent inquiries to inform the decision without guessing:
- **BOM audit** (inline, complete) — what does `qqq-bom` manage today vs what's missing? Found: 5 internal qqq modules missing from BOM, 20 QBits + qbit-build-parent + qctl not in BOM, 4 different qbit-build-parent versions across QBits (version drift), qqq-app-starter stale by ~18 versions, template QBits hard-code qqq 0.35.0, one QBit is an uncustomized duplicate, and `${revision}` is reactor-scoped so it can't extend cross-repo.
- **Multi-repo patterns research** (agent, blocked) — Quarkus vs Spring Cloud vs Camel vs Bazel vs Turborepo/Nx — what have mature Java+TS ecosystems converged on? Needs web access.

The user paused here via `/gsd-pause-work` rather than push through. Next session: pick up with the research unblock, decide scope, then finish init.

</context>

<next_action>

**Start with:** Try `WebFetch` on a trivial URL (e.g., `https://example.com`) to detect whether permissions are now granted. If it works, re-spawn the research agent with the prompt preserved in `HANDOFF.json` → `research_state.multi_repo_patterns` — use `subagent_type=general-purpose`, `model=sonnet`, `run_in_background=true`. If the fetch is denied, ask the user to grant `WebSearch` + `WebFetch` (via `/permissions` or `settings.json`) and wait.

In parallel, commit the BOM audit file so it doesn't get lost: 
```
gsd-sdk query commit "docs: qqq-bom coverage audit" .planning/research/bom-audit.md
```
(Actually — the pause commit below already captures this in the WIP commit; skip the separate commit on resume if WIP already included it.)

Once research lands, re-present the deferred scope question with research + BOM audit facts in hand.

</next_action>
